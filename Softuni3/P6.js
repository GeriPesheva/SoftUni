function textNum(input ) {
    let furstName = input[0];
    let lastName = input[1];
    let age = input[2];
    let town = input[3];
console.log(` You are ${furstName } ${lastName}, a ${age}-years old person from ${town}. `)
}
textNum(["Maria", "Ivanova",20, "Sofia"])